import { VideoPanelDirective } from './video-panel.directive';

describe('VideoPanelDirective', () => {
  it('should create an instance', () => {
    const directive = new VideoPanelDirective();
    expect(directive).toBeTruthy();
  });
});
